# Base URL of the external candidate records API consumed by /candidates.
NEXT_PUBLIC_API_URL=http://localhost:4000

# Base URL of the in-repo TrackFlow identity API (services/main.py).
# Login, registration, /auth/me and /profiles/me are served from here.
NEXT_PUBLIC_AUTH_API_URL=http://localhost:8000

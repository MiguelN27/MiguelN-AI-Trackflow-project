// interfaces and types right here, here we'll go all the interfaces for the main entities of the company defined in context.md

/* INterface model:

interface Entity {
    id: string;
    department: string;
    createdAt: Date;
    updatedAt: Date;
    location: string;
    manager: string;
    team: number;
}

*/ 